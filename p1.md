# 文档定位

《FusionEngine Message Specification》是一份面向设备集成、通信协议解析和导航数据处理开发者的**二进制消息协议规范**。当前文档版本为 **0.25，发布日期 2026-03-13**，对应 FusionEngine Protocol Release **v1.26.0**，全文 138 页。它主要规定 Point One Navigation 的 FusionEngine 软件如何：

* 向外输出定位、速度、姿态、GNSS 卫星、传感器和设备状态；
* 接收外部 IMU、轮速、里程计和外部位姿测量；
* 接收配置、重启、启停、数据导入导出等控制命令；
* 在串口、TCP、UDP、WebSocket、文件和 UNIX 域套接字等接口上控制消息输出；
* 与 NMEA、RTCM 和 ROS 数据结构配合使用。

它并不是导航融合算法原理说明，而是详细规定了“每个字节应该如何组织和解释”。文档同时建议优先使用官方的 C++/Python `fusion-engine-client` 库，而不是自行从零实现整个协议。

---

# 一、FusionEngine 协议的基本定位

FusionEngine 使用紧凑的二进制消息传输以下信息：

* PVT 解算，即位置、速度和时间；
* 车辆或平台的航向、俯仰、横滚；
* GNSS 卫星与单个信号的跟踪、使用和修正状态；
* IMU、轮速、轮编码器和车辆速度；
* RTK、SSR、SBAS、L-band 等差分或精密修正状态；
* 系统版本、设备标识、日志和事件；
* 设备配置与持久化数据。

同一协议同时支持**主机到设备的输入消息**和**设备到主机的输出消息**。第 3 章几乎占据全文主体，按命令、导航结果、传感器数据和状态信息分类定义了几十种消息。

---

# 二、二进制消息结构

## 1. 总体结构

每条消息由两部分组成：

```text
Message Header，固定 24 字节
+
Message Payload，可变长度
```

第 7 页的示意图显示，通信流中会连续出现“头部—载荷—头部—载荷”。每条完整消息都按 **4 字节边界对齐**，因此载荷末尾可能包含填充字节。

所有多字节字段均采用：

```text
小端序 / Little Endian / Intel Byte Order
```

文档定义了 `bool`、`u8/u16/u32`、`i8/i16/i32/i64`、`f32/f64` 和变长字符串等基础类型。普通 `str` 字符串通常**不以空字符结尾**，必须结合长度字段解析。

## 2. 24 字节消息头

消息头字段依次包括：

| 字段                |  类型 | 作用                     |
| ----------------- | --: | ---------------------- |
| Sync Byte 0       |  u8 | 固定为 `0x2E`，即 ASCII `.` |
| Sync Byte 1       |  u8 | 固定为 `0x31`，即 ASCII `1` |
| Reserved          |  2B | 保留                     |
| CRC               | u32 | 消息 CRC-32              |
| Protocol Version  |  u8 | 当前线上协议版本固定为 2          |
| Message Version   |  u8 | 当前消息结构版本               |
| Message Type      | u16 | 消息类型编号                 |
| Sequence Number   | u32 | 每发送一条消息递增              |
| Payload Size      | u32 | 后续载荷字节数                |
| Source Identifier | u32 | 消息来源标识                 |

同步字节合起来就是：

```text
2E 31
```

因此流式解析器通常可以先搜索这两个字节来寻找帧起点。

真正决定载荷如何解析的是：

```text
Message Type + Message Version
```

文档要求新版本消息尽量通过利用旧版保留字段或在末尾扩展字段来保持向后兼容。因此旧解析器通常可以忽略新版本消息末尾不认识的字段。

## 3. Source Identifier

`Source Identifier` 的具体意义可能随消息类型变化：

* 测量输入消息通常设置为 `0`；
* 不涉及测量源的普通消息通常设置为 `0xFFFFFFFF`；
* 多传感器系统可用它区分不同设备或测量来源。

---

# 三、CRC 校验

CRC 字段使用标准 CRC-32 的反向生成多项式：

```text
0xEDB88320
```

CRC 计算范围不是整个消息，而是：

```text
从 Protocol Version 字段开始
一直到消息最后一个字节，包括整个 Payload
```

也就是说，前面的同步字节、保留字段和 CRC 字段本身不进入计算范围。

文档给出的算法采用：

```text
初始值：0
计算前：与 0xFFFFFFFF 异或
逐字节查 CRC 表
计算后：再次与 0xFFFFFFFF 异或
```

实现发送端时，通常先组装从 Protocol Version 到 Payload 末尾的所有内容，再计算 CRC 并回填消息头。

---

# 四、时间系统

## 1. P1 Time

FusionEngine 内部使用统一时间基准：

```text
Point One Time，简称 P1 Time
```

P1 时间用于关联 IMU、GNSS、轮速和导航解算结果。但它具有几个重要特点：

* 通常以设备启动或内部时钟为参考；
* 可能来自 MCU 计时器或操作系统单调时钟；
* 会根据内部时钟模型动态调整；
* 不应假定它等于 GPS 时间；
* 不应假定它等于 POSIX/Unix 时间；
* 也不应假定它严格等于设备原始 MCU 或 OS 时钟。

## 2. Timestamp 结构

通用 `Timestamp` 长度为 8 字节：

```text
Seconds   u32
Fraction  u32
```

其中：

* `Seconds` 是自相应历元以来的整数秒；
* `Fraction` 是纳秒部分；
* 无效值使用 `0xFFFFFFFF`。

同一个结构可以表示：

* P1 时间；
* GPS 时间，历元为 1980-01-06；
* POSIX 时间，历元为 1970-01-01；
* 平台特定的系统时间。

必须结合 `SystemTimeSource` 判断其含义。

## 3. SystemTimeSource

时间源枚举主要包括：

|     值 | 含义               |
| ----: | ---------------- |
| `0x0` | 时间戳无效，设备在接收时打时间戳 |
| `0x1` | P1 时间            |
| `0x2` | 设备在接收消息时使用系统时间打戳 |
| `0x3` | 外部发送方的单调系统时钟     |
| `0x4` | GPS 时间           |

## 4. MeasurementDetails

所有外部传感器输入和原始传感器输出通常都以一个 20 字节的 `MeasurementDetails` 开头，包含：

* Measurement Time；
* Time Source；
* Sensor Data Source；
* 保留字段；
* Output P1 Time。

输入测量时，发送方不应填写 `Output P1 Time`；该字段会被设备忽略。FusionEngine 收到消息后，会通过内部时钟模型将外部时间转换成 P1 时间。

文档特别强调：若外部数据没有准确采样时间，只能在“消息到达设备时”打时间戳，那么传感器采样、传输和接口缓存造成的未知延迟会直接影响融合性能。

---

# 五、传感器来源、GNSS 类型和坐标系

## 1. SensorDataSource

传感器来源可标记为：

* Unknown；
* Internal：设备内部 IMU 或 GNSS；
* Hardware IO：硬件电平或脉冲；
* CAN；
* Serial；
* Network。

这使输出端能够判断某组轮速或里程计数据究竟来自软件输入、CAN 总线还是硬件脉冲。

## 2. GNSS 星座

`SatelliteType` 支持：

* GPS；
* GLONASS；
* Galileo；
* BeiDou；
* QZSS；
* SBAS；
* IRNSS/NavIC。

同时保留了 LEO 和 Mixed 枚举，但当前未使用。

每个星座值都可对应一个位掩码。例如配置启用 GPS 和 Galileo 时，可以组合相应位。

## 3. GNSS 频段

频段分类包括：

* L1；
* L2；
* L5；
* L6；
* L1-L2 Wide Lane；
* L1-L5 Wide Lane；
* S Band。

这里的“频段”不是单一卫星系统专属频率，而是把可由同一射频前端或天线频段覆盖的多个系统信号归为一类。例如 L1 类别中包括 GPS L1、Galileo E1、BeiDou B1 和 GLONASS G1。

## 4. 信号名和信号分量

文档分别为 GPS、GLONASS、Galileo、BeiDou、SBAS 和 QZSS 定义信号名称，例如：

* GPS L1 C/A、L1C、L2C、L5；
* Galileo E1、E5a、E5b、E6；
* BeiDou B1I、B1C、B2I、B2a、B2b、B3I；
* QZSS L1C、L2C、L5、L6。

信号还可区分：

* Combined；
* Data；
* Pilot。

## 5. GNSSSignalType

单个 `GNSSSignalType` 将以下信息打包为一个 16 位值：

```text
SatelliteType        4 bit
Reserved             2 bit
FrequencyBand        4 bit
Reserved             1 bit
SignalName           3 bit
GNSSComponent        2 bit
```

因此它不仅能表示“GPS L5”，还可以精确表示：

* GPS L5-I 数据分量；
* GPS L5-Q 导频分量；
* Galileo E1-B 数据分量；
* Galileo E1-C 导频分量等。

## 6. 车辆坐标系和姿态定义

第 28 页给出的车辆坐标系是：

```text
+X：车辆前方
+Y：车辆左方
+Z：车辆上方
```

姿态相对于局部 ENU 坐标系定义：

* Yaw：绕 Z 轴右手旋转，零度从东开始，正值为逆时针；
* Pitch：绕 Y 轴右手旋转，正俯仰表示车头向下；
* Roll：绕 X 轴右手旋转，正横滚表示车辆向右侧倾斜。

从真北方向计算航向角时：

```text
heading = 90° - yaw
```

采用内禀 Euler 3-2-1 旋转，顺序必须是：

```text
Yaw → Pitch → Roll
```

这和部分航空、机器人或汽车软件中“航向零度从北开始”的习惯不同，是集成时最容易出现的错误之一。

---

# 六、I/O 接口和数据类型

## 1. InterfaceID

一个接口 ID 由以下内容组成：

* TransportType；
* 接口索引；
* 两字节保留字段。

支持的接口类型主要包括：

* Serial；
* File；
* TCP；
* UDP；
* WebSocket；
* UNIX Domain Socket；
* Current，即当前命令所使用的接口；
* All，即所有接口通配符。

文档 v0.25 的版本记录中特别说明，本版修正了 TCP 的 `TransportType` 枚举值。

## 2. Stored DataType

设备可导入、导出的存储数据包括：

* `CALIBRATION_STATE`：校准状态；
* `CRASH_LOG`：崩溃诊断信息；
* `FILTER_STATE`：滤波器状态；
* `USER_CONFIG`：用户配置。

## 3. 车辆挡位

`GearType` 包括：

* Unknown；
* Forward；
* Reverse；
* Park；
* Neutral。

轮速、车辆速度和编码器消息中经常包含此字段，因为速度值是否带方向与挡位信息会影响车辆航迹推算。

---

# 七、命令和控制消息

主机发送给设备的命令主要位于 13000～132xx 编号范围。

## 1. MessageRequest（13001）

要求设备立即输出指定消息。

如果该消息类型不支持按请求输出，设备返回 `CommandResponseMessage`，响应码为 Value Error。

常用于主动获取：

* 版本信息；
* 设备 ID；
* 支持的接口列表；
* 某些状态消息。

## 2. ResetRequest（13002）

使用一个 32 位复位掩码，可以组合复位不同状态：

* 重启导航引擎；
* 清除 GNSS 修正；
* 清除 GNSS 时间；
* 清除位置、速度和姿态；
* 清除星历；
* 清除快速 IMU 误差估计；
* 清除全部导航状态和训练数据；
* 清除校准数据；
* 恢复出厂配置；
* 重启 GNSS 测量引擎；
* 重启导航处理器；
* 执行诊断日志复位。

还定义了几个常用组合：

| 名称            |           掩码 | 主要效果                        |
| ------------- | -----------: | --------------------------- |
| HOT_START     | `0x00000001` | 仅重新启动导航引擎，尽量保留位置、时间、星历和校准   |
| WARM_START    | `0x00000201` | 重启导航并清除星历                   |
| COLD_START    | `0x00000FFF` | 清除运行状态、时间、位置姿态、快速 IMU 修正和星历 |
| POSE_RESET    | `0x000001FB` | 重点重置运动学位置、速度和姿态             |
| FACTORY_RESET | `0xFFFFFFFF` | 状态、校准和用户配置全部恢复出厂            |

## 3. ShutdownRequest（13005）

可分别：

* 停止导航引擎并将状态写入非易失存储；
* 停止当前日志。

Flags 为 0 时按“停止导航引擎”处理。

## 4. StartupRequest（13008）

可：

* 启动导航引擎；
* 结束当前日志并立即开启新日志。

为兼容旧实现，Flags 为 0 时仍会启动导航引擎。

## 5. FaultControl（13006）

这是测试和工厂诊断消息，文档明确禁止在生产环境中使用。

它可以人为触发：

* 设备崩溃；
* Fatal Error；
* COCOM 加速度、速度或高度限制；
* 禁用 GNSS 测量；
* 模拟区域黑障；
* 工厂线路测试模式；
* 完整性状态故障。

触发 Crash 后设备会立即停止工作，因此可能来不及返回命令响应。

---

# 八、配置系统

## 1. SetConfigMessage（13100）

该消息用于设置单个配置项，基本结构是：

```text
ConfigType
Flags
Value Length
Value
```

主要 Flags：

* `APPLY_AND_SAVE`：应用后立即写入持久化存储；
* `REVERT_TO_DEFAULT`：恢复该项的活动值和保存值为默认值。

主要配置项包括：

### 安装和几何参数

* Device Lever Arm：IMU 相对车辆坐标系的位置；
* Device Orientation：IMU 的粗略安装方向；
* GNSS Lever Arm：主 GNSS 天线位置；
* Output Lever Arm：希望输出位置所对应的车辆点；
* GNSS Aux Lever Arm：双天线系统的副天线位置；
* Vehicle Details：车型、轴距、前轮距、后轮距。

车辆配置中预置了一些 CAN 车型配置，例如 Tesla、Audi、BMW、Volkswagen、Rivian 等，但文档提示，不同制造年份的 CAN 定义可能不同。

### 软件轮速配置

`WheelConfig` 用于通过软件消息或 CAN 输入轮速、轮编码器数据，配置内容包括：

* 轮速传感器类型；
* 使用后轮、前轮、所有车轮或整车速度；
* 转向轮类型；
* 测量更新周期；
* 轮编码器输出周期；
* 转向比；
* 每个 Tick 对应距离；
* Tick 回绕最大值；
* Tick 是否有符号；
* 倒车时 Tick 是否仍持续增加。

文档建议优先使用：

```text
左右后轮的差分轮速或 Tick + 挡位信息
```

并规定同一时刻只应向设备提供一种轮速或 Tick 测量类型。

### 硬件轮编码器配置

`HardwareTickConfig` 用于设备直接采集外部脉冲电平：

* 关闭、上升沿或下降沿捕获；
* 可选方向电平；
* 正向高有效或正向低有效；
* 米/脉冲比例。

它不能与软件轮速/CAN 配置混用。

### GNSS 配置

* 启用的 GNSS 星座掩码；
* 启用的频段掩码；
* UTC 闰秒覆盖；
* GPS 10 位周数回绕覆盖；
* 电离层模型；
* 对流层模型。

电离层模型支持 Auto、Off、Klobuchar、SBAS；对流层模型支持 Auto、Off 和 Saastamoinen。

### I/O 接口配置

`InterfaceConfigSubmessage` 可针对特定接口设置：

* 是否输出诊断消息；
* 串口波特率；
* TCP/UDP 远程地址；
* UNIX 域套接字路径；
* 网络端口；
* 接口启用/禁用；
* Client/Server 方向；
* UNIX Socket 的 Stream、Datagram 或 Sequenced Packet 模式。

### 其他配置

还包括：

* 看门狗开关；
* 用户设备标识；
* 性能分析掩码；
* L-band 解调器中心频率、搜索窗、服务 ID、数据率、解扰器等。

旧的 UART 专用波特率和诊断开关配置已经被标记为弃用，建议使用通用 `InterfaceConfig`。

## 2. GetConfigMessage（13101）

查询配置项，可选择：

* ACTIVE：当前运行中的配置；
* SAVED：持久化存储中的配置。

设备用 `ConfigResponseMessage` 返回值。

## 3. SaveConfigMessage（13102）

支持三种操作：

* Save：保存当前所有活动配置；
* Revert to Saved：恢复到已保存配置；
* Revert to Default：活动配置和保存配置都恢复默认值。

## 4. ImportData / ExportData

用于在主机与设备之间传输：

* 校准状态；
* 崩溃日志；
* 滤波器状态；
* 用户配置。

导出的内容由 `PlatformStorageDataMessage（13113）` 返回，其中包括数据类型、状态、配置来源、数据版本、长度和实际字节流。

---

# 九、消息输出速率控制

## 1. SetMessageRate（13220）

可以针对指定接口、协议和消息设置输出频率。

支持的协议类型：

* FusionEngine；
* NMEA；
* RTCM；
* ALL。

频率枚举包括：

* Off；
* On Change；
* 10、20、40、50、100、200、500 ms；
* 1、2、5、10、30、60 秒；
* Device Default。

支持接口、协议和消息 ID 通配符，可以一次性修改一整类消息。

两个重要标志：

* `APPLY_AND_SAVE`：设置后立即保存；
* `INCLUDE_DISABLED_MESSAGES`：批量修改时也处理当前被禁用的消息。

如果接口启用了诊断输出，实际频率可能覆盖用户配置，因此文档区分：

* Configured Rate；
* Effective Rate。

## 2. GetMessageRate（13221）

查询指定接口和消息的活动或已保存输出频率，设备返回 `MessageRateResponse（13222）`。

## 3. SupportedIOInterfacesMessage（13223）

报告设备实际支持的接口列表。不同硬件能力可能明显不同，例如某些完整设备可支持多个 TCP socket，而嵌入式模块可能只有两个固定 UART。

---

# 十、命令响应

## 1. CommandResponseMessage（13000）

包含原命令的 Sequence Number 和响应码。

响应码包括：

* OK；
* 不支持该消息版本；
* 不支持该功能；
* 参数值错误；
* 存储空间不足；
* 执行失败；
* Payload 长度不一致；
* 数据损坏；
* 没有已存储数据；
* 当前不可用；
* 不支持该接口。

## 2. ConfigResponseMessage（13103）

返回：

* 配置来源；
* 活动值是否与保存值不同；
* ConfigType；
* 响应码；
* 返回值长度；
* 配置值。

## 3. PlatformStorageDataMessage（13113）

返回导出的设备存储数据，并能够区分：

* POSIX 平台 FusionEngine 配置；
* 嵌入式 FusionEngine 配置；
* 嵌入式 SSR Client；
* 桌面或库形式的 SSR Client。

---

# 十一、导航解算输出

## 1. PoseMessage（10000）

这是最核心的导航结果消息，内容包括：

* P1 Time；
* GPS Time；
* Solution Type；
* Stationary 标志；
* 大地水准面起伏；
* WGS-84 纬度、经度和椭球高；
* 东、北、天位置标准差；
* Yaw、Pitch、Roll；
* 姿态标准差；
* 车辆前、左、上方向速度；
* 速度标准差；
* 三维、水平和垂直保护级别。

解算类型包括：

* Invalid；
* Autonomous GPS；
* DGPS；
* RTK Fixed；
* RTK Float；
* Integrate，即惯性或运动模型推算；
* Visual；
* PPP。

RTK Fixed 表示载波相位整周模糊度已固定，是列出的 GNSS 解算中精度最高的一类。

## 2. GNSSInfoMessage（10001）

描述本次 Pose 解算使用的 GNSS 情况：

* GPS 时间；
* UTC 闰秒；
* 使用的卫星数；
* 最近修正数据年龄；
* 与基站的距离；
* 基站 ID；
* GDOP、PDOP、HDOP、VDOP；
* GPS 时间估计标准差。

## 3. GNSSSatelliteMessage（10002）

旧式的逐卫星信息，包括：

* 星座；
* PRN；
* 是否用于位置解算；
* C/N0；
* 方位角；
* 高度角。

从 v0.24 开始，该消息被标记为旧式/弃用，推荐使用更完整的 `GNSSSignalsMessage`。

## 4. GNSSSignalsMessage（10006）

这是新的逐卫星、逐信号状态消息。它包含两个数组：

```text
GNSSSatelliteInfo[N]
GNSSSignalInfo[S]
```

卫星级信息包括：

* 星座、PRN；
* 是否用于解算；
* 是否不健康；
* 是否为非视距信号；
* 是否有星历；
* 是否有 SBAS 修正；
* 高度角和方位角。

信号级信息包括：

* 完整 GNSSSignalType；
* PRN；
* C/N0，单位为 0.25 dB-Hz；
* 伪距是否有效或被使用；
* Doppler 是否有效或被使用；
* 载波相位是否有效或被使用；
* 整周模糊度是否固定；
* 是否有 RTK、SBAS 修正或星历。

## 5. PoseAux（10003）

提供 PoseMessage 之外的辅助量：

* 车辆前、左、上方向的位置标准差；
* ENU 位置协方差；
* 姿态四元数；
* ENU 速度；
* ENU 速度标准差。

## 6. CalibrationStatus（10004）

报告 IMU 校准过程：

* 当前阶段；
* 估计的安装 Yaw/Pitch/Roll 偏差；
* 相应标准差；
* 累计行驶距离；
* 导航状态是否已验证；
* 陀螺仪偏置完成度；
* 加速度计偏置完成度；
* 安装角完成度；
* 完成校准所需的距离和误差门限。

## 7. RelativeENUPosition（10005）

报告移动站相对于本地 RTK 基站的 ENU 坐标差，包括：

* 基站 ID；
* 东、北、上相对位置；
* 各方向标准差；
* 解算类型和时间。

该消息不是双天线姿态消息；双天线相关信息应使用 GNSSAttitude 系列消息。

---

# 十二、校正后的传感器输出

文档将这一组称为“主要传感器输出”，通常最适合上层直接使用。

## IMUOutput（11000）

输出已经完成以下处理的 IMU 数据：

* 加速度计和陀螺仪偏置修正；
* 比例因子修正；
* 校准和滤波；
* 从 IMU 原始安装坐标系旋转到车辆坐标系。

包含三轴加速度、三轴角速度及各自标准差。

## GNSSAttitudeOutput（11005）

使用双天线或多天线相对定位计算车辆姿态：

* Yaw、Pitch、Roll；
* 各姿态标准差；
* 主副天线基线长度及标准差；
* 解算类型。

只有正确配置主、副 GNSS 天线杆臂，输出才会代表车辆本体姿态。

## WheelSpeedOutput（11135）

输出校正后的四轮速度：

* 左前、右前、左后、右后；
* 数据来源；
* 挡位；
* 是否为有符号速度。

## VehicleSpeedOutput（11136）

输出校正后的整车纵向速度。对于硬件轮脉冲，设备可能在较长时间范围内累计 Tick，以提高速度精度，因此输出频率不一定等于原始输入脉冲频率。

---

# 十三、外部传感器输入

使用软件输入传感器前，必须先通过 `SetConfigMessage` 配置相应传感器类型和几何参数。文档建议同一时间只使用一种轮速或 Tick 输入形式。

## 1. IMUInput（11004）

输入原始 IMU：

* 温度；
* 三轴加速度；
* 三轴角速度；
* MeasurementDetails。

数据使用定点缩放：

* 加速度约为 `m/s² × 2^-16`；
* 角速度约为 `rad/s × 2^-20`；
* 温度约为 `°C × 2^-7`。

## 2. ExternalPoseInput（11007）

这是 v0.25 新增的重要消息，允许外部系统向 FusionEngine 提供：

* ECEF 位置；
* ECEF 位置标准差；
* ENU 下的 Yaw/Pitch/Roll；
* 姿态标准差；
* ENU 速度；
* 速度标准差。

每个部分都可以是可选的。它可以用于：

* 帮助导航引擎初始化；
* 作为外部定位信息增强融合结果；
* 在设置重置标志后重新初始化位置、速度和姿态。

输入必须对应已经配置的车辆输出点，而不是任意传感器安装点。

## 3. WheelTickInput（11103）

输入四轮累计编码器 Tick。

## 4. VehicleTickInput（11104）

输入代表整车前向运动的单个累计 Tick。

## 5. WheelSpeedInput（11105）

输入四轮速度，速度值采用定点缩放，支持有符号/无符号标志。

## 6. VehicleSpeedInput（11106）

输入单个整车前向速度。文档提醒，只使用某一个轮子的速度作为整车速度，在转弯时可能造成较差效果。

---

# 十四、原始传感器输出

原始输出专门用于离线处理、诊断或重新融合。它们不进行：

* 偏置修正；
* 比例因子修正；
* 安装角旋转；
* 标定补偿。

唯一额外处理通常是计算对应的 P1 时间。

包括：

* RawIMUOutput（11002）；
* RawGNSSAttitudeOutput（11006）；
* RawWheelTickOutput（11123）；
* RawVehicleTickOutput（11124）；
* RawWheelSpeedOutput（11125）；
* RawVehicleSpeedOutput（11126）。

`RawGNSSAttitudeOutput` 输出的是主、副天线之间的 ENU 相对位置向量，而不是已经修正成车辆姿态角的结果。

## InputDataWrapper（13120）

用于记录传感器、RTK 修正流等输入数据的**原始二进制内容**，以便诊断和日志回放。载荷长度需要从消息头的 Payload Size 推断。

---

# 十五、设备状态与诊断

## 1. SystemStatus（10500）

当前定义较简单，主要包含：

* P1 时间；
* GNSS 接收机温度；
* 大量预留字段。

## 2. SSRStatus（10501）

详细报告 SSR/OSR 修正链路：

* 当前 GPS 周和周内时间；
* 最近输出修正对应的 GPS 时间；
* 修正来源是外部 OSR 基站还是 SSR 模型生成；
* RTCM 基站 ID 和坐标；
* 修正中包含的卫星和信号数量；
* 包含哪些 GNSS 星座和信号；
* 各系统可用星历数量；
* OSR、SSR 数据状态；
* 当前 SSR 网格 ID；
* SSR 模型组件启用、解码和可用状态。

SSR 组件可分别表示：

* 网络元数据；
* 网格定义；
* 卫星组定义；
* 大地水准面模型；
* 天线改正；
* 高速和低速卫星修正；
* 全球、区域、网格化电离层；
* 网格或区域对流层。

## 3. VersionInfoMessage（13003）

返回：

* 固件版本；
* FusionEngine 软件版本；
* 操作系统、内核或 Bootloader 版本；
* 底层 GNSS 测量引擎版本。

字符串由长度字段限定，不以 `\0` 结尾。

## 4. EventNotificationMessage（13004）

用于日志和事件通知，事件类型包括：

* LOG；
* RESET；
* CONFIG_CHANGE；
* COMMAND；
* COMMAND_RESPONSE。

包含系统时间、Flags、字符串长度和事件说明。

## 5. DeviceIDMessage（13007）

返回：

* 设备类型；
* 硬件 ID；
* 用户 ID；
* GNSS ID。

设备类型枚举包括 Point One Atlas、Quectel LG69T 系列、Nexar Beam2K 和 Point One SSR Client 等。

---

# 十六、GNSS 修正、ROS 和硬件专用消息

## 1. LBandFrameMessage（14000）

输出 L-band 解调得到的数据帧，包含：

* 非 P1 同步的系统接收时间；
* 数据长度；
* 比特错误数；
* 信号功率；
* 多普勒频偏；
* 实际帧内容。

## 2. ROS 支持

ROS 消息包括：

* ROSPose（12000）；
* ROSGPSFix（12010）；
* ROSIMUMessage（12011）。

这些消息设计为便于复制到 ROS 对应结构中，但文档明确说明：

```text
它们不保证与 ROS 消息在内存或字节布局上兼容，
不能直接进行结构体强制转换。
```

ROSPose 使用相对于第一个有效 ROS Pose 的局部 ENU 位置和姿态四元数。

ROSGPSFix 包含：

* WGS-84 位置和高度；
* 航迹角、地速、爬升速度；
* GPS 时间；
* DOP；
* 三维、水平、垂直误差；
* 速度和时间误差；
* ENU 位置协方差。

ROSIMUMessage 包含：

* 姿态四元数；
* 姿态协方差；
* 三轴角速度及协方差；
* 三轴加速度及协方差。

其中未知数据和未知协方差按 ROS 语义使用 `NAN`、`0` 或 `-1` 表示。

## 3. STA5635 专用消息

针对 STMicroelectronics STA5635 射频前端，定义：

* STA5635Command（14100）；
* STA5635CommandResponse（14101）；
* STA5635IQData（14102）。

可用于寄存器访问和 IQ 原始采样抓取，并不适用于所有 FusionEngine 设备。

---

# 十七、NMEA-0183 支持

FusionEngine 支持部分标准 NMEA：

* GGA；
* GLL；
* GSA；
* GSV；
* RMC；
* VTG；
* ZDA。

还定义了 Point One 和 Quectel 私有语句。

## $P1CALSTATUS

输出校准状态：

```text
$P1CALSTATUS,
<stage>,
<state_verified>,
<gyro_percent_complete>,
<accel_percent_complete>,
<mounting_angle_percent_complete>
```

## $P1MSG

输出设备日志或事件：

```text
$P1MSG,<system_time_ns>,<severity>,<message>
```

数字越小通常表示严重程度越高。

## Quectel 私有语句

包括：

* `$PQTMGNSS`；
* `$PQTMVER`；
* `$PQTMVERNO`。

但本版中它们的字段仍被写成 `TBD`，并没有给出完整格式。

文档强调，NMEA 只覆盖 FusionEngine 二进制协议功能的一小部分，若需要完整的姿态、协方差、信号级状态和配置能力，应使用二进制协议。

---

# 十八、v0.25 的主要变化

版本历史显示，v0.25 相比 v0.24 主要有两项变化：

1. 新增 `ExternalPoseInput（11007）`；
2. 修正 TCP 的 `TransportType` 枚举值。

此前 v0.24 的重要变化包括：

* 新增 GNSSSignalType 及相关枚举；
* 新增 GNSSSignalsMessage；
* 将旧的 GNSSSatelliteMessage 标记为弃用；
* SSRStatus 开始使用新的各星座 SignalName 掩码。

---

# 十九、实际开发时最重要的注意事项

1. **不要混淆版本号。**
   封面所说的 Release v1.26.0 与消息头中固定为 2 的 `Protocol Version` 不是同一个层级；具体消息还各有自己的 `Message Version`。

2. **解析必须使用 Message Type 和 Message Version。**
   不能仅根据 Payload 长度猜测消息。

3. **按小端序处理所有数值。**

4. **完整消息长度必须为 4 的倍数。**
   变长字符串或数组之后可能存在 Padding。

5. **CRC 不包含同步字节和 CRC 字段。**

6. **P1 时间不是 GPS 时间，也不是 Unix 时间。**

7. **外部测量应尽量使用真实采样时间。**
   “接收时打戳”会引入未知通信延迟。

8. **区分校正输出和原始输出。**
   正常上层应用通常使用 `IMUOutput`、`WheelSpeedOutput` 等校正数据；离线标定才使用 Raw 消息。

9. **双天线姿态依赖正确的主、副天线杆臂配置。**

10. **轮速配置、车辆尺寸和输入消息必须匹配。**
    WheelSpeed、WheelTick、VehicleSpeed 和 VehicleTick 不应同时混合输入。

11. **动态字符串通常不是空字符结尾。**
    必须严格使用长度字段，只有少数固定配置字符串明确要求 `\0` 结尾。

12. **生产环境不要使用 FaultControl。**

13. **优先以官方库的实现为准。**
    PDF 中存在几处明显疑似排版或编号错误，例如：

    * `ExportDataMessage` 标题为 13111，但正文的 Message ID 写成 13222，而 13222 实际又用于 `MessageRateResponse`；
    * `CalibrationStatus` 的 Message Version 被写成 `1S`；
    * 公共 `DataVersion` 定义为 32 位结构，但部分消息表中标记成 `u16`。

    因此从零实现时，最好对照官方 `fusion-engine-client` 源码和真实设备数据进行验证。
